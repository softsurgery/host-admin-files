import { auth } from "./auth";
import { workspace } from "./workspace";
import { file } from "./file";
import { upload } from "./upload";

export const api = {
  auth,
  file,
  upload,
  workspace
};
